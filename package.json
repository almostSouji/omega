{
	"name": "omega-rules",
	"version": "0.4.7",
	"description": "SIEM rules for JS objects",
	"author": "almostSouji <timoqueezle@gmail.com>",
	"license": "MIT",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"build:clean": "del-cli dist",
		"build:check": "tsc --noEmit",
		"build:types": "tsc --emitDeclarationOnly",
		"build:esm": "swc ./src --out-dir ./dist --strip-leading-paths",
		"build": "yarn build:clean && yarn build:check && yarn build:esm && yarn build:types",
		"start": "node dist/index.js",
		"start:dev": "npm run build && npm run dev",
		"dev": "node --enable-source-maps dist/index.js",
		"test": "yarn build && vitest run",
		"test:watch": "yarn build && vitest watch",
		"prepublish": "tsc",
		"lint": "yarn biome lint",
		"format": "yarn biome format --write"
	},
	"dependencies": {
		"chevrotain": "^13.0.0",
		"octokit": "^5.0.5",
		"re2": "^1.26.1",
		"readdirp": "^5.0.0",
		"yaml": "^2.9.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"@commitlint/cli": "^21.2.1",
		"@commitlint/config-angular": "^21.2.0",
		"@swc/cli": "^0.8.1",
		"@swc/core": "^1.15.46",
		"@swc/helpers": "^0.5.23",
		"@types/node": "^22.10.2",
		"del-cli": "^7.0.0",
		"lefthook": "^2.0.13",
		"rimraf": "^6.1.3",
		"typescript": "^5.7.2",
		"vitest": "^4.1.10"
	},
	"engines": {
		"node": ">=24.0.0"
	},
	"packageManager": "yarn@4.5.3"
}
